#include <bits/stdc++.h>
using namespace std;

int f[11] = {0, 1, 2, 2, 2, 2, 6, 6, 6, 6, 6};

int main()
{
    // freopen("input.txt", "r", stdin);

    int t, n;
    cin >> t;
    for (int i = 1; i <= t; i++) {
        cin >> n; cout << f[n];s
    }

    return 0;
}